# JSON → Astro Host

Утилита для локального развёртывания **Astro-сайта из JSON-файла**.

Проект парсит JSON, в котором описаны файлы и их содержимое, восстанавливает файловую структуру Astro-проекта и позволяет локально захостить сайт на вашей машине.

---

## Что нужно делать?

Скопировать твой **json** в **input.json**. Далее запустить следующие команды:
```
cd ./json_host
node generate.mjs
cd output
npm install # может занять какое то время, минуты 2
npm run dev
```

сайт будет доступен по `http://localhost:8000/`


## 📌 Что делает проект

- 📄 Читает JSON со списком файлов
- 📁 Создаёт директории и файлы на диске
- 🧩 Поддерживает:
  - строковый контент (код)
  - JSON-объекты (`package.json`, `tsconfig.json` и т.д.)
- 🚫 Игнорирует служебные поля (например `telegram id`)
- 🌐 Запускает локальный Astro-сервер
- 🧹 Автогенерируемые файлы **не попадают в Git**

---

## 📂 Формат входного JSON

```json
{
  "files": [
    { "telegram id": "7052064142" },
    {
      "name": "package.json",
      "content": {
        "name": "example",
        "scripts": {
          "dev": "astro dev"
        }
      }
    },
    {
      "name": "src/pages/index.astro",
      "content": "<h1>Hello Astro</h1>"
    }
  ]
}


